import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.electrictamarindive',
  appName: 'electric-tamarin-dive',
  webDir: 'dist'
};

export default config;
